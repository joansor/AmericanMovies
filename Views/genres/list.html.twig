{% extends "template.html.twig" %}
{# enfant de actors #}

{% block body %}
{{ dump(genres) }}




<div class="container">

		<div class="row"> <!-- Début row -->

			<div class="col-12 p-4 bg-light text-dark">
			
				Genres → {{ MoviesByGenres.genre_du_film }}
 
			</div> 

		</div>

	</div>

    <div class="container bg-light text-dark mt-1 mb-3">

    <h1>Films - Listing de tous les films du site</h1>

        <a href="{{baseUrl}}/films/add" class="float-right btn btn-sm btn-success">Ajouter un film</a> 

        <div class="row col-md-12">

        {% for genre in genres %}

            <div class="col-md-4">

                <a href="{{baseUrl}}/films/show/{{ MoviesByGenres.id_f }}">
                
                    <img class="img-thumbnail" src="{{baseUrl}}/{{ MoviesByGenres.poster_f }}" alt="image film"/>
                    
                   
                    
                </a>

            </div>

            {% endfor %}

        </div>

    </div>

{% endblock %}
